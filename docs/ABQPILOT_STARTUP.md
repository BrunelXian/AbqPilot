# AbqPilot Startup

AbqPilot-v2 starts as a deterministic dry-run scaffold. The old V1 project is read-only reconnaissance and is not part of this repository.

Use only:

```powershell
D:\XianLab\envs\conda\LangChainEnv\Scripts\python.exe
```

Run tests:

```powershell
D:\XianLab\envs\conda\LangChainEnv\Scripts\python.exe -m pytest -q
```

MVP-01 is limited to one target region, `NT11`, mean aggregation, `500 +/- 20 C`, and `heat_flux_scale` repair.

